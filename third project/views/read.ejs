<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Document</title>
   <link rel="stylesheet" href="/stylesheet/style.css">
   <script src="https://cdn.tailwindcss.com"></script>

</head>
<body class="text-white">
   <h1 class="text-5xl mb-5">All Users</h1>
   <a href="/" class="text-2xl text-blue-500 underline mb-4">back to home</a>
   

       <div class="flex flex-wrap gap-5 w-full ">
         <% if (users.length>0) { %>
            <% users.forEach(user => { %>
   
               <div class="w-[18%] bg-slate-700 p-2  rounded-lg">
                  <div class="flex flex-col justify-center items-center gap-2">
                  <div class="w-full h-72  rounded-lg bg-slate-500 overflow-hidden "><img src="<%= user.imageUrl %>" alt="loadig..." class="w-full h-full object-cover"></div>
                  <p class="text-2xl text-center"><%= user.name %></p>
                  <p class="text-xl text-gray-300 text-center"><%= user.email %></p>
                    <div class="flex justify-center items-center gap-2 ">  <a href="/delete/<%= user.id%>" class="bg-red-500 p-2 rounded-lg hover:bg-red-600 hover:text-white">delete</a>
                      <a href="/edit/<%= user.id%>" class="bg-blue-500 p-2 rounded-lg hover:bg-blue-600 hover:text-white">edit</a></div>
                  </div>
             </div>
   
   
               <%})%>
            <% }else{ %>
                <p class="text-2xl text-center text-red-500">no data found </p> 
               <% } %>
   
   
        
       </div>

    
   </div>
   
</body>
</html>