<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Document</title>
   <link rel="stylesheet" href="/stylesheet/style.css">
   <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-800 text-white p-4">

   <div class="w-full flex justify-end items-center gap-4">
      <div class="w-14 h-14 rounded-full bg-red-500 overflow-hidden">
         <img src="/images/uploads/<%=user.profilepic%>" alt="" class="w-full h-full object-cover "> </div>
      <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded-md hover:bg-red-600">Logout</a>
   </div>
      <h1 class="text-4xl font-bold text-center ">Hello <%=user.name%></h1>
      <p class="text-center mt-10">You can create the post </p>

      <div class=" w-full flex flex-col items-center justify-satrt h-fit p-2">
         <form action="/post" method="post" class="bg-gray-900 p-4 rounded-md flex flex-col gap-4 mt-10 w-1/3">
            <textarea class="bg-gray-700 w-full outline-none resize-none text-white px-4 py-2 rounded-md" name="content" placeholder="Enter What your mind "></textarea>
            <button class="bg-blue-500 text-white px-4 py-2 rounded-md" type="submit">Create Post</button>
         </form>
      </div>
      <h1 class="text-4xl font-bold text-center mt-10">Your Posts</h1>

      <div class="w-full flex gap-4 items-start justify-center  mt-10 flex-wrap">

        
        <% if(user.posts.length>0){%>

         <%user.posts.reverse().forEach(post =>{ %>
            <div class="w-1/4 bg-gray-700 p-4 rounded-md  flex flex-col gap-4 mt-10">
               <h1 class="text-blue-500 font-bold">@<%=user.name%></h1>
               <p><%=post.content%></p>
   
               <div class="btn flex justify-start items-end gap-8">
                  <div class="flex flex-col gap-2 items-center">
                     <small>likes(<%=post.likes.length%>)</small>
                        <a href="/like/<%=post._id%>" class=" text-blue-600 rounded-md">
                           <%if(post.likes.includes(user._id)){%>
                              <span>unlike</span>
                           <%}else{%>
                              <span>like</span>  
                           <%}%>
                        </a>
                  </div>
                  
                  <div><a href="/edit/<%=post._id%>" class=" text-gray-300 text-gray-400 rounded-md">Edit</a></div>
               </div>
           </div>
            
            <%})%>

         <%} else { %>
            <h1 class="text-4xl font-bold text-center mt-10">No posts found</h1>
         <%}%>
        
      
       
         </div>
    
    

   <script src="/javascripts/script.js"></script>
</body>
</html>